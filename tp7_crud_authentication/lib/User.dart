class User {
  String email;
  String password;
  User(this.email, this.password);
  void setEmail(String email) {
    this.email = email;
  }

  void setPassword(String pass) {
    this.password = pass;
  }
}
